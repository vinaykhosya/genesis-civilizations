{
  "name": "genesis-root",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter portal dev",
    "build": "pnpm --filter portal build && node -e \"const fs=require('fs'); fs.cpSync('portal/.vercel/output', '.vercel/output', {recursive: true})\"",
    "start": "pnpm --filter portal start",
    "lint": "pnpm --filter portal lint",
    "publish": "pnpm --filter portal publish"
  },
  "workspaces": [
    "portal"
  ]
}
